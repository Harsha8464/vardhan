def is_prime(num):
    if num < 2:
        return False
    for i in range(2, num):
        if num % i == 0:
            return False
    return True

def first_prime():
    number = 2
    while True:
        if is_prime(number):
            return number
        number += 1

print(first_prime())
